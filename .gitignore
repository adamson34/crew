.claude/settings.local.json
.DS_Store
*.code-workspace

# Runtime state and config (generated per-engagement by installer)
.crew
.crew-state.yaml

# Engagement artifacts (contain client data) — anchored to repo root so this
# doesn't also match crew/workflows/assessment/ or similarly-named paths
/engagement/
/assessment/
/deliverables/
